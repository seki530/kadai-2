
  <h1>Editing Book</h1>
  <% if @books.errors.any? %>
  <%= @books.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @books.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

  <%= form_with model: @books do |f| %>

  <h4><lavel for="title">Title</lavel></h4>
  <%= f.text_field :title %>

  <h4><lavel for="body">Body</lavel></h4>
  <%= f.text_area :body %>

  <p><%= f.submit 'Update Book' %></p>
  <% end %>
 <td><%= link_to"Show",book_path(@books.id) %></td>
|
  <a href="/books">Back</a>
